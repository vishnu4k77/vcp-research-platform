# Backtesting package — signal simulation and performance analytics.
#
# Public API:
#   BacktestService   — SQL read layer (entries + forward price data)
#   BacktestEngine    — trade simulation (stop / target / timeout exit logic)
#   PerformanceMetrics — aggregate metrics over a trade DataFrame
